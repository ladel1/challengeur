{% set menu = [
  {
    'route':"app_main_index",
    'label':"Accueil"
  },
  {
    'route':"app_main_aboutus",
    'label':"A propos"
  },
  {
    'route':"app_challenge_add",
    'label':"Ajouter un défi"
  },
  {
    'route':"app_challenge_list",
    'label':"Challenges"
  }    
] %}
<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Challenge</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          {% for item in menu %}          
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path(item.route) }}">{{ item.label|upper }}</a>
            </li>
          {% endfor %}
        </ul>
      </div>
    </div>
  </nav>